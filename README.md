# RADIX Talent Match

A candidate-facing skill-matching app that allows candidates to match their skills to job descriptions.

## Setup

### Backend
```bash
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt # Or install packages manually
uvicorn main:app --reload
```

### Frontend
```bash
cd frontend
npm install
npm run dev
```
